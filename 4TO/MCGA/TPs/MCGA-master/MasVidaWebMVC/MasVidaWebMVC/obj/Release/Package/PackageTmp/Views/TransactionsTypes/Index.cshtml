@model IEnumerable<MasVidaWebMVC.TransactionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TransactionTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransactionTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TransactionTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransactionTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransactionTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransactionTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransactionTypeID })
        </td>
    </tr>
}

</table>
